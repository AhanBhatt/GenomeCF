# Environment availability

Default local environment:

- Windows CPU path for smoke tests, registry validation, quickstart reproduction, website build, and paper generation.

Documented accelerated path:

- WSL2/Linux CUDA for Caduceus-Ph and heavier foundation-model runs.

Environment artifacts:

- `environment.yml`
- `envs/caduceus.yml`
- `Dockerfile`
- `apptainer.def`
- `docs/CADUCEUS_SETUP.md`
