This folder keeps lightweight public helper assets.

Large local model checkpoints are intentionally excluded from Git. For local runs, place them in:

- `../local_runtime_assets/external/`

Examples:

- `../local_runtime_assets/external/dnabert2_local/`
